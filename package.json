{
  "name": "seep",
  "version": "1.0.0",
  "description": "Structured Expert Elicitation Platform",
  "main": "index.js",
  "scripts": {
    "docker:dev": "docker compose --env-file .env.dev -f docker-compose-dev.yml up --build -d",
    "docker:prod": "docker compose --env-file .env.prod up --build -d"
  },
  "author": "PRIORB",
  "license": "NPOSL-3.0",
  "dependencies": {
    "nodemon": "^3.1.7"
  }
}
